#include <stdio.h>
/**
 * main - Entry point
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
int a;
for (a = 97; a <= 123; a++)
{
putchar(a);
}
int A;
for (A = 65; A <= 90; A++)
{
putchar(A);
}
putchar('\n');
return (0);
}
